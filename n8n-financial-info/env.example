# env.example - Template de variáveis de ambiente para Fly.io
# Copie este arquivo e configure as variáveis no Fly.io usando flyctl secrets

# SEGURANÇA (OBRIGATÓRIO)
N8N_ENCRYPTION_KEY=change-me-to-a-random-string-min-10-chars

# Autenticação básica (opcional mas recomendado)
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=change-me-strong-password

# URLs (ajustar com seu domínio Fly.io)
N8N_HOST=n8n-financial-info.fly.dev
N8N_PROTOCOL=https
WEBHOOK_URL=https://n8n-financial-info.fly.dev/

# Timezone
GENERIC_TIMEZONE=America/Sao_Paulo
TZ=America/Sao_Paulo

# Configurações de execução
EXECUTIONS_PROCESS=main
EXECUTIONS_MODE=regular
EXECUTIONS_TIMEOUT=3600
EXECUTIONS_TIMEOUT_MAX=3600

# Logs
N8N_LOG_LEVEL=info
N8N_LOG_OUTPUT=console

# Performance
N8N_CONCURRENCY_PRODUCTION_LIMIT=10

# Interface
N8N_EDITOR_BASE_URL=https://n8n-financial-info.fly.dev
N8N_METRICS=false

# Google APIs (suas credenciais)
# GOOGLE_GEMINI_API_KEY=your-key-here
# GOOGLE_SHEETS_CLIENT_ID=your-client-id
# GOOGLE_SHEETS_CLIENT_SECRET=your-client-secret
# GMAIL_CLIENT_ID=your-gmail-client-id
# GMAIL_CLIENT_SECRET=your-gmail-client-secret

# ===================================
# Como usar essas variáveis no Fly.io
# ===================================
#
# Use o comando flyctl secrets set para configurar:
#
# flyctl secrets set N8N_ENCRYPTION_KEY=$(openssl rand -base64 32)
# flyctl secrets set N8N_BASIC_AUTH_ACTIVE=true
# flyctl secrets set N8N_BASIC_AUTH_USER=admin
# flyctl secrets set N8N_BASIC_AUTH_PASSWORD=SuaSenha123
# flyctl secrets set GOOGLE_GEMINI_API_KEY=sua-key
#
# Para ver as secrets configuradas:
# flyctl secrets list

